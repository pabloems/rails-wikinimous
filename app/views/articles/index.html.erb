<div class="container-fluid">

  <h1>Coffe Type</h1>
  <div class="btn btn-secondary">
    <a <%= link_to  "New Coffe", new_article_path %>>
  </div>
</div>
<div class="container">
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article) %></h2>
    <%= article.content %>
  <% end %>
</div>